# Supabase project credentials (Project Settings -> API).
# Copy this file to `.env.local` and fill in real values. `.env.local` is gitignored.
# With these UNSET, the app runs in demo mode (localStorage), unchanged.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Auth-slice seeded demo users (email / password):
#   counselor@demo.school / Password123!   (role: admin)
#   student@demo.school    / Password123!   (role: student)
